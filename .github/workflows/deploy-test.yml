name: Deploy Test (SSH)

on:
  push:
    branches:
      - main

jobs:
  deploy-test:
    name: SSH deploy test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H 31.128.42.135 >> ~/.ssh/known_hosts

      - name: SSH deploy test (no side effects)
        run: |
          ssh -i ~/.ssh/id_ed25519 deploy@31.128.42.135 << 'EOF'
            set -e

            echo "👤 User:"
            whoami

            echo "📁 Project directory:"
            cd /home/deploy/zachot

            echo "📦 Git status:"
            git status --short

            echo "🧠 Runtime versions:"
            node -v || echo "node not installed"
            python3 --version

            echo "📂 Project contents:"
            ls -la | head -n 20

            echo "✅ SSH deploy test finished successfully"
          EOF
