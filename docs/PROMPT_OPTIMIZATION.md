# Оптимизация промптов для снижения затрат

## 📊 Текущее состояние

### Анализ длины промптов

**Проблемные промпты (требуют оптимизации):**

1. **`humanize_100`** - самый длинный промпт (~2000 токенов)
   - Содержит избыточные повторения
   - Можно сократить на 40% без потери качества

2. **`humanize_75`, `humanize_50`** - средние по длине (~1500 токенов)
   - Дублируют инструкции из `humanize_100`
   - Можно унифицировать базовую часть

3. **`content` промпт** - используется для каждой главы
   - Генерируется 10-15 раз за одну работу
   - Оптимизация даст максимальный эффект

## 🎯 Стратегия оптимизации

### 1. Сокращение промптов (экономия 30-40%)

#### Принципы:
- Убрать повторяющиеся инструкции
- Использовать более короткие формулировки
- Объединить похожие требования

#### Пример оптимизации `humanize_100`:

**Было (2000 токенов):**
```
Твоя задача — переписать текст так, чтобы он выглядел написанным живым человеком...
[много повторяющихся инструкций]
```

**Стало (1200 токенов):**
```
Переписать текст под живого автора. Ключевые изменения:
- Ритм: варьировать длину предложений (3-25 слов)
- Синтаксис: использовать разные конструкции
- Лексика: заменить клише на естественные фразы
- Модальность: добавить авторскую позицию
- Несовершенства: допустить мелкие стилистические вариации
```

### 2. Иерархия промптов (экономия 20-30%)

Создать базовый промпт + дельты для уровней:

```python
BASE_HUMANIZE = """
Переписать текст под живого автора.
Базовые правила: [...]
"""

HUMANIZE_DELTAS = {
    0: "",  # Без изменений
    25: "+ Легкое сглаживание",
    50: "+ Естественный стиль",
    75: "+ Авторская манера",
    100: "+ Максимальная индивидуальность"
}
```

### 3. Few-shot вместо long-shot (экономия 40-50%)

Вместо длинных объяснений - короткие примеры:

**Было:**
```
Используй разные длины предложений. Короткие для акцента. Длинные для развития мысли...
```

**Стало:**
```
Примеры:
- "Это важно. Потому что влияет на результат."
- "Это важно, поскольку непосредственно влияет на конечный результат работы."
```

### 4. Кэширование промптов (экономия 50-70%)

Использовать Redis для кэширования:
- Структура работы → кэш на 1 час
- Источники → кэш на 2 часа
- Классификация → кэш на 30 минут

**Уже реализовано** ✅ для классификации

### 5. Динамическая детализация

Адаптировать промпт под сложность:

```python
if complexity == "student":
    prompt = SHORT_PROMPT  # 500 токенов
elif complexity == "professional":
    prompt = DETAILED_PROMPT  # 1000 токенов
```

## 📈 Ожидаемый эффект

### Экономия по этапам:

| Этап | Текущие токены | После оптимизации | Экономия |
|------|----------------|-------------------|----------|
| Структура | 800 | 500 | 37% |
| Источники | 1000 | 600 | 40% |
| Контент (×10) | 15000 | 9000 | 40% |
| Humanize | 2000 | 1200 | 40% |
| QC | 800 | 500 | 37% |
| **ИТОГО** | **19600** | **11800** | **40%** |

### Финансовый эффект:

При 100 генерациях в день:
- Текущие затраты: ~$200/день
- После оптимизации: ~$120/день
- **Экономия: $80/день = $2400/месяц**

## 🛠 План реализации

### Фаза 1: Быстрые победы (1-2 часа)
1. ✅ Кэширование классификации (реализовано)
2. Сократить `humanize_*` промпты на 40%
3. Оптимизировать `content` промпт

### Фаза 2: Структурные изменения (3-4 часа)
4. Внедрить иерархию промптов
5. Добавить few-shot примеры
6. Динамическая детализация по сложности

### Фаза 3: A/B тестирование (1 неделя)
7. Сравнить качество старых vs новых промптов
8. Собрать метрики (токены, качество, время)
9. Выбрать лучшие варианты

## 📝 Конкретные действия

### 1. Оптимизировать humanize_100

```python
# apps/api/data/prompts.json
"humanize_100": """
Переписать текст под живого автора с максимальной индивидуальностью.

ИЗМЕНЕНИЯ:
1. Ритм: чередовать короткие (3-7 слов) и длинные (15-25 слов) предложения
2. Синтаксис: варьировать структуры (простые, сложные, вопросы)
3. Лексика: заменить клише естественными фразами
4. Модальность: добавить авторскую позицию (считаю, полагаю, очевидно)
5. Несовершенства: допустить стилистические вариации

ПРИМЕРЫ:
До: "Данный факт свидетельствует о важности проблемы."
После: "Это важно. Почему? Потому что влияет на результат."

Текст: {text}
"""
```

### 2. Создать базовый промпт для контента

```python
"content_base": """
Написать раздел "{section_title}" для {work_type}.

Тема: {topic}
Объём: ~{target_words} слов
Стиль: {complexity_level}

Требования:
- Академический стиль
- Логичная структура
- Опора на источники

{previous_context}
"""
```

### 3. Добавить кэширование источников

```python
# apps/api/services/text_generation_service.py
@cache_service.cached("ai:sources", ttl=7200)  # 2 часа
async def generate_sources(self, generation: Generation):
    # ...
```

## 🎓 Лучшие практики

1. **Короче = лучше**: Каждое слово должно нести смысл
2. **Примеры > Объяснения**: Few-shot эффективнее long-shot
3. **Кэшировать всё**: Повторные запросы должны быть бесплатными
4. **Тестировать**: A/B тестирование для проверки качества
5. **Мониторить**: Отслеживать токены через Prometheus

## 📊 Метрики для отслеживания

```python
# apps/api/monitoring/metrics.py
prompt_tokens_saved = Counter(
    'prompt_tokens_saved_total',
    'Total tokens saved by optimization',
    ['optimization_type']
)

prompt_quality_score = Gauge(
    'prompt_quality_score',
    'Quality score of generated content',
    ['prompt_version']
)
```

## ✅ Чеклист внедрения

- [ ] Сократить humanize_* промпты на 40%
- [ ] Оптимизировать content промпт
- [ ] Добавить кэширование источников
- [ ] Внедрить иерархию промптов
- [ ] Добавить few-shot примеры
- [ ] Настроить A/B тестирование
- [ ] Добавить метрики оптимизации
- [ ] Провести недельный тест
- [ ] Зафиксировать лучшие варианты

---

**Статус**: 🟡 В процессе
**Приоритет**: 🔥 Высокий (экономия $2400/месяц)
**Ответственный**: Backend Team
**Дедлайн**: 2 недели
